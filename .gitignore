# Security — never commit credentials or tokens
.env
.env.*
*.env
*secret*
*token*
*credential*
*config.json
!package.json

# macOS
.DS_Store
*.DS_Store

# Editor
.vscode/

# --- SECRETS-POLICY standard block (added 2026-05-29) ---
# See /Projects/SECRETS-POLICY.md
*.pem
*.key
*-token.txt
*-credentials.json
push-*.sh
secrets.*
*.swp
.COMMIT_EDITMSG
*.bak

# Hub docs — strategy, build logs, planning (never deploy to GitHub Pages)
_hub/
